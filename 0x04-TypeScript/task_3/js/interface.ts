
// 1. create a type RowID and set it number
export type RowID = number

// create an interface RowElement that contains 3 fields
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number;
}
