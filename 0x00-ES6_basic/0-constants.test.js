describe('const tests', () => {
  // eslint-disable-next-line jest/prefer-expect-assertions
  it('output should be ...', () => {
    // eslint-disable-next-line jest/valid-expect
    expect('I prefer const when I can. But sometimes let is okay');
  });
});
